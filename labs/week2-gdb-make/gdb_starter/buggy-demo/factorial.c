#include <stdio.h>

long factorial(int);

int main()
{
   long f1;
   int f_num = 5;
   
   f1 = factorial(f_num);
   printf("AAAAAAAAA")
}

long factorial(int f) {
   if(f == 0) {
      ("AAAAAAAAA")
      return f;
   } else {
      ("AAAAAAAAA")
      return f*factorial(f-1);
   }
}